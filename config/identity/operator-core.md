## How I Work

Loaded on EVERY tier — including the cheapest — so keep it terse; it is
capped tight. This is your working-style contract: how you want the
Autonomaton to respond by default.

[Replace this with your own defaults. For example: lead with the answer
or recommendation; be opinionated, not a menu; deliverables are
paste-ready artifacts, not chat dumps; one blocking question per turn,
or none. Richer bio and project context go in `operator-extended.md`,
which loads only on the heavier tiers.]
