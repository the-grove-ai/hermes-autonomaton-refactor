# GRV-009 E5 C-VERBS (declarative-resolution-v1) — grouped verb record.
# family=code_execution  zone=yellow  tiers=[2, 3]  trigger=code_generation/debugging/system_admin
id: execute_code
kind: verb
trigger:
  intents:
  - code_generation
  - debugging
  - system_admin
  keywords: []
  dock_affinity: []
  always: false
bindings:
  tools:
  - execute_code
  credentials: null
  toolset_key: code_execution
tier_rule:
  eligible:
  - 2
  - 3
  preferred: 2
  promotion_criteria: {}
  validation:
    strategy: shadow_compare
    confidence_threshold: 0.95
    shadow_window: 20
zone: yellow
telemetry:
  feed: intent_feed
  track:
  - invocation
  - result
  - cost
  - latency
  - confidence
  - human_feedback
context:
  disclosure: eager
  payload: 'Native code_execution tools (write, zone yellow). Tools: execute_code.

    '
  dock_composition: none
lifecycle:
  state: approved
  provenance: migrated
  created_at: '2026-06-12T00:00:00+00:00'
  last_used: null
  use_count: 0
  flywheel_eligible: true
lineage:
  source_patterns: []
  parent_id: null
  decision_log:
  - actor: operator
    timestamp: '2026-06-12T00:00:00+00:00'
    from_state: migrated
    to_state: approved
    reason: 'E5 C-VERBS — declarative-resolution-v1: grouped verb record (strict A2: zone+tier+trigger
      homogeneous) governing native tools at the operator-locked gate'
    evidence:
    - config/tool_groups.yaml
    - config/zones.schema.yaml#tool_zones
failure:
  fallback: halt_and_surface
  diagnostic_context:
  - 'capability: execute_code'
  - 'subsystem: native verb registry (tools.registry)'
  - 'check: tool registered + zone mapped in zones.schema.yaml'
  circuit_breaker:
    threshold: 3
    window_seconds: 300
