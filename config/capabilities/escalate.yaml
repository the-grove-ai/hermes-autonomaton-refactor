# GRV-009 E5 C-VERBS (declarative-resolution-v1) — grouped verb record.
# family=null:misc  zone=yellow  tiers=[1, 2, 3]  trigger=always
id: escalate
kind: verb
trigger:
  intents: []
  keywords: []
  dock_affinity: []
  always: true
bindings:
  tools:
  - escalate
  credentials: null
  toolset_key: null
tier_rule:
  eligible:
  - 1
  - 2
  - 3
  preferred: 1
  promotion_criteria: {}
  validation:
    strategy: shadow_compare
    confidence_threshold: 0.95
    shadow_window: 20
zone: yellow
telemetry:
  feed: intent_feed
  track:
  - invocation
  - result
  - cost
  - latency
  - confidence
  - human_feedback
context:
  disclosure: eager
  payload: 'Native misc tools (write, zone yellow). Tools: escalate.

    '
  dock_composition: none
lifecycle:
  state: approved
  provenance: migrated
  created_at: '2026-06-12T00:00:00+00:00'
  last_used: null
  use_count: 0
  flywheel_eligible: true
lineage:
  source_patterns: []
  parent_id: null
  decision_log:
  - actor: operator
    timestamp: '2026-06-12T00:00:00+00:00'
    from_state: migrated
    to_state: approved
    reason: 'E5 C-VERBS — declarative-resolution-v1: grouped verb record (strict A2: zone+tier+trigger
      homogeneous) governing native tools at the operator-locked gate'
    evidence:
    - config/tool_groups.yaml
    - config/zones.schema.yaml#tool_zones
failure:
  fallback: halt_and_surface
  diagnostic_context:
  - 'capability: escalate'
  - 'subsystem: native verb registry (tools.registry)'
  - 'check: tool registered + zone mapped in zones.schema.yaml'
  circuit_breaker:
    threshold: 3
    window_seconds: 300
